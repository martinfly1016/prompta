# Session Log — 2026-05-05

> 一天内从 Day-0 可行性 push 到完整 freemium AI tool 上线，含支付 / 认证 / 邮件全套。
> 15 个 commit，覆盖 7 个产品阶段 + 6 个改进/修复 + 1 个内容采集运营。

## Major Delivery — `/tools/personal-color-analysis`

prompta.jp 第一个交互式 AI 工具。命中日本市场 30K+/月 SEO 关键词（パーソナルカラー診断 写真 9.9K / 無料 9.9K / カメラ 8.1K）。

### 5 阶段产品形态（按上线顺序）

1. **静态预览**（`de3035b`） — Hero + 上传 UI + 16 色 mock palette + FAQ + 关联 prompt CTA。Day-0 测试 5 张照片 × 3 重复证明 4-season + undertone 5/5 稳定 → ✅ GO
2. **配额闸门**（`c70e64f`） — 3 次/日免费，cookie + IP+UA hash 24h 滑动窗口双判，IP cap 5/日防 shared IP 误伤
3. **Stripe Checkout 付费**（`df36f7f`） — ¥300 / 10 credits 买断包；DB 加 PaidCredits / StripePayment 模型；Webhook 幂等增 credits + 设 cookie；自动通过 `_setup-stripe-personal-color.ts` 创建 product/price/webhook
4. **Gemini Vision 真实诊断**（`dd94c66`） — `gemini-2.5-flash` 结构化 JSON 输出，refund-on-failure 防双扣；前端 phase-aware 加载文案
5. **邮件恢复 credits**（`f2fa88b`） — AgentMail 复用，HMAC 签名 1h token；modal 底部「以前購入したクレジットがある場合 →」入口
6. **NextAuth 三 provider 登录**（`e83d79e`） — Credentials（admin 保留）+ Google OAuth + Email Magic Link（custom sendVerificationRequest 走 AgentMail）；登录后 sync-credits 自动桥接 cookie
7. **购买确认 + invoice 邮件**（`b88dfd1`） — Stripe receipt + AgentMail 欢迎邮件双发，黄色警示框明示「クレジットは {email} 紐付け」

### 改进与修复（按时间顺序）

- `f0e3ff2` — Copy / Try / View 点击埋点（GA4 event + DB increment，4 自定义维度）
- `97aab3a` — Privacy + Terms 法务页（日文初版）+ sitemap 加 personal-color + Header nav 加 featured 入口
- `3a17560` — 16 色 swatch 名字截断修复（横向 → 垂直堆叠）
- `6662c49` — **关键修复**：登录态 Stripe Checkout 锁定 `customer_email`，防 Link 用 saved wallet 替换（实测过一次 byte-ad ↔ gmail 错配，DB 手动迁移 + 加防御）
- `44ade90` — 上传图片深度校验（magic byte + sharp metadata + 尺寸/动画），**配额前**校验，失败 422 不扣费
- `10658a7` — 结果区 Portal 到 upload card 外面（之前被 max-w-3xl 锁住，缩到 768px 导致名字截断；现在 max-w-5xl 1280px 同 mock 宽）
- `1cdace3` — 解析中加载 UX：照片扫描光带 + 5 步轮播文案 + 进度条 + 弹跳 dots，遮蔽 15-25s 等待

### 运营

- `c915899` — photo-edit May 2026 月度入库（8 条新 prompt + 5 PASS 样片，外部 agent 做 SemRush 关键词调研报告）

## Stripe Live 资源（prod-only，稳定）

| 资源 | ID |
|---|---|
| Product | `prod_UScJCa1c1IaUEU`「パーソナルカラー診断 10回パック」 |
| Price | `price_1TTh4hRY2GlDjBwNHbxAfLYf`（¥300 JPY one-time） |
| Webhook endpoint | `we_1TTh4iRY2GlDjBwNJ2bFoU3u`（事件 `checkout.session.completed`） |

## Vercel 新增 env vars

- `STRIPE_SECRET_KEY` / `STRIPE_PRICE_ID` / `STRIPE_WEBHOOK_SECRET`
- `GOOGLE_AI_API_KEY`（Gemini Vision）
- `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET`（NextAuth Google OAuth）
- `AGENTMAIL_API_KEY`（既有，复用 magic link + credits 恢复 + 购买邮件）

## 用户必须手动做的待办

- [ ] **Rotate Google OAuth Client Secret** — 用户在对话里贴过给 agent 配 Vercel；去 Cloud Console → 認証情報 → OAuth client → Reset Secret → Vercel Edit Value（注意 Edit Value 不是 Sensitive flag）→ Redeploy
- [ ] **GSC 手动请求索引** 4 个 URL：
  1. `/tools/personal-color-analysis` ⭐ 最重要
  2. `/tools`（内容更新）
  3. `/legal/privacy` / `/legal/terms`
  - IndexNow 已自动推 Bing/Yandex（Google 不通 IndexNow，必须 GSC 控制台手动点）
- [ ] **Google OAuth 同意画面填 URL**：现在 `/legal/privacy` 和 `/legal/terms` 上线了，填上去消除 unverified 警告 + 合规
- [ ] **Stripe Dashboard → Settings → Customer emails** → 检查「Successful payments」开关（匿名购买路径也走 receipt）

## 已知未设计边界（用户问到再做）

1. 多邮箱合并到一个 User 账户（cookie 只能绑一个 emailHash → 邮箱 A 买 + 邮箱 B 登录混用要靠手动恢复来回切）
2. Admin 调账面板（目前 SQL 改 PaidCredits）
3. 跨账户转赠 credits
4. 退款（Stripe Dashboard 手动退，PaidCredits 不自动减）

## 下次会话候选起点

- 内容营销：写「パーソナルカラー診断 AI で写真からあなたのカラーを見つける方法」guide（命中长尾）
- 多邮箱合并 / Admin credits 面板（如果有真实运营痛点）
- D 候选：GA4 自定义维度配置 + retention 报表
- E 候选：performance 优化 / Lighthouse 跑分
