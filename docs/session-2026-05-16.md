# Session 2026-05-16 — 日报诊断 → 参数化扩量 → 内容采集

单日会话主线：
- **AM**: 跑 5/9-5/15 日报 → 发现 paywall_view 19 次异常 + checkout_started=1 但 Stripe 0 成交 → 追根因（Phase 0 后 PaywallTrigger 误归类 + owner 测试污染 + 5/10 plumbing 三连发）
- **AM/PM**: 修 3 bug + 上线 param-monitor §8（换正确指标 — engagement 替代 copy-rate）+ 加 3 个 anime config 放大样本 + GA4 admin 注册 4 维度后扩 §8.5
- **PM**: `/collect-content` 一轮（lexica 中止 + midjourney 15 入库）+ 3 tag 批准 + seoIntro backfill
- **EVE**: 候选池规模评估 → 用户选 A 路径 → Batch A: 19 独立 config + 3 alias = 22 slug 净增（总覆盖 19 → 41 slug）

**7 commits 全部 push，main 已上线 + 15 新 prompt 入库 + 3 新 approved tag**。

---

## 时间线

| Hash | Title | 核心改动 |
|---|---|---|
| `9c03f8b` | fix(tracking): split PaywallTrigger by blockReason | track.ts `PaywallTrigger` 加 `login_required` + 两个 Gate 按 `state.blockReason` 分流 |
| `85d227c` | feat(data-analys): param panel engagement monitor + owner exclusion | 新 `param-monitor.ts` + `_owner_exclusion.ts` 共享清单；tool-usage / paywall-hits / users-detail 统一口径 |
| `695e4d3` | feat(prompt-params): +3 high-traffic anime slug configs | anime-prompt-f061b77b / anime-girl-scene-10 / anime-woman-neon-08（覆盖从 16 → 19 slug）|
| `0b41c6d` | feat(data-analys): param-monitor §8.5 per-param breakdown | 新维度注册后查询 `customEvent:prompt_slug/param_id/param_value` + 容错 (not set) 旧事件 |
| `a98b48e` | docs(session): single-day handoff 2026-05-16 | 早段 handoff（之后 collect + batch A 在下方 Part F/G 补充） |
| _collect-content_ | 15 MJ prompt 入库 | offset=784 段 lexica 中止后切 MJ（DB 写入直接，无 git commit）+ 3 tag approve + seoIntro |
| `f4a99bd` | feat(prompt-params): +19 configs + 3 aliases for Tier-1 traffic | 22 slug 净增 → 总覆盖 19→41 slug，预期参数化 sessions 95→~280-320 |

---

## Part A — 日报数据异常的 3 层根因排查

### 1. personal-color paywall_view 19 次 vs DB 仅 1 anon exhausted

**Root cause**: Phase 0 (5/11 `df57544`) 把 `/api/tools/personal-color/check` 的 `canUse` 重定义为：
```ts
const canUse = !!eh && balance > 0
const blockReason = !eh ? 'login_required' : balance > 0 ? 'none' : 'credits_exhausted'
```

但 `PersonalColorQuotaGate.tsx:295` 的 `handlePickFile` 还是老语义：
```ts
if (!state.canUse) {
  openPaywall(setShowModal, 'exhausted_pick')   // ← 不分场景
}
```

→ **未登录用户点「写真を選択」按钮** = 触发 `paywall_view.exhausted_pick`，但他们一次 free 都没用过。19 次里 18 次是这种「未登录路过」用户，**1 次是真撞墙**（5/9 04:58-05:00 anon `5e225236-edf2` 3 次 free in 1 分 22 秒）。

trigger 名 "exhausted_pick" 的语义在 Phase 0 后已死。**真实数据上「拉新阻力」和「复购阻力」混在一起**，view→click 4.8% 不可执行。

**Fix (`9c03f8b`)**: PaywallTrigger 加 `'login_required'`，按 `state.blockReason` 分流：
```ts
const trigger: PaywallTrigger =
  state.blockReason === 'login_required' ? 'login_required' : 'exhausted_pick'
```

未来 GA4 切片：
- `login_required` → 新用户拉新阻力（改 modal 文案、考虑去掉 sign-in gate）
- `exhausted_pick` → 复购阻力（改价格 / plan 设计）

### 2. hair-color 7 天 7 次 paid usage 全是 owner 测试

DB 查 `martinfly1016@gmail.com` 5/11 09:44-10:00 共 7 次 paid 调用（NB2 模型选型测试，与 commit `ccde468` 时间窗匹配）。但 `db-query.ts:95` 的 `testEmails` 只排除 `e2e-test+t{1,2,3}@prompta.jp`，没排除 owner 真实账号。

**Fix (`85d227c`)**: 抽 `_owner_exclusion.ts` 共享清单，加 `martinfly1016@gmail.com` + `yuchao@byte-ad.com`。tool-usage / paywall-hits / `_paywall_users_detail.ts` 统一引用。

修正后：
- hair-color 7d 真实外部 paid usage: **0 次**（不是 7 次）
- vs 上周 21 次（修正后）= -100%

### 3. checkout_started=1 但 Stripe 0 成交

GA 显示 5/10 同日 fire 了三个事件：
```
2026-05-10 paywall_view.trigger=plumbing_test  hair-color  1
2026-05-10 paywall_purchase_click               hair-color  1
2026-05-10 checkout_started                     hair-color  1
```

`paywall_purchase_click` / `checkout_started` 事件 schema **没有 trigger 字段**（看 `track.ts:119-125`），所以单看 funnel 表无法区分。但按时间戳对照 `docs/session-2026-05-10.md`：

> plumbing test：用 chrome-devtools MCP fire 测试事件...（注：5/13 看日报会有 1 行 trigger=plumbing_test，是测试污染，可忽略）

实际上当时 fire 了 funnel 全部三个事件做端到端测试，**没真的请求 `/api/checkout/hair-color`**（所以 Stripe 没生成 session）。session-2026-05-10.md 漏标了 click + checkout 也是同批污染。

→ **本周真实 funnel**: 20 view（剔除 plumbing 1）→ 0 click → 0 checkout → 0 pay。

未修代码（一次性污染数据，无法修复历史；新事件 trigger 字段会带 `login_required` / `exhausted_pick` 分流）。

---

## Part B — 用错指标了：copy-rate ≠ 参数化的合适指标

第一次 param 效果分析对照组：

| 组 | sessions | copy-rate |
|---|---|---|
| 参数化 16 slug | 71 | 11.27% |
| 非参数化 top-15 | 76 | 11.84% |

→ 几乎无差异。一度判定为「机制可能没真正提升转化」。

**用户洞察纠错**：copy_prompt 是「最终决策」事件 — 用户调一次 vs 调十次都只 copy 一次（找到满意 prompt 后走人）。**copy-rate 量错了东西**。

正确指标：**用户在页面上的投入度** — bounce / engage / dwell。

### 重做后的数据（2026-05-10 ~ 2026-05-16, 7d）

| 组 | sessions | bounce | engage | dwell |
|---|---|---|---|---|
| 参数化 16 slug | 71 | **32.4%** | **67.6%** | 78.5s |
| 非参数化（所有 prompt 页）| 234 | 41.0% | 59.0% | 389s\* |

**Δ bounce**: -8.6pp 🟢 · **Δ engage**: +8.6pp 🟢

（\*dwell 受少数「开 tab 不关」用户拉偏，bounce / engage 才是干净对比）

调参 slug 单独看更清楚：
- twintail-blonde-sailor-outfit: bounce 16.7% / engage 83.3%
- chibi-girl-walk-29: bounce 20.0% / engage 80.0% / 6.5 events/user
- straight-long-black-hair-glossy: bounce 20.0% / dwell 170.9s / 9.0 events/user

**3 个独立指标交叉验证机制有效**：
| 价值假设 | 量度 | 数据 | 结论 |
|---|---|---|---|
| 用户注意到面板 | 触发率 | 14.1% (10/71 sessions) | ✅ |
| 用户认真在试 | events/调参用户 | 4.5 次 | ✅ |
| 用户更投入 | bounce 差 | -8.6pp | ✅ |

**Plan B (DB 加 paramsJson 列) 暂缓** — 当前 drop-a-file workflow 已经够轻量，痛点不在 "admin 无需 redeploy"。真正的瓶颈是「哪些 slug 该参数化」。

---

## Part C — param-monitor.ts §8 + §8.5 (`85d227c` + `0b41c6d`)

新脚本 `src/scripts/data-analys/param-monitor.ts`：
- slug 列表从 `getConfiguredSlugs()` 动态拉，不手维护
- 输出 Markdown + JSON 双格式
- 支持 `--days=N`（默认 7）

SKILL.md 改动：
- `daily-report` 执行步骤加 K) 调用
- 输出格式加 §8「Prompt 参数化效果监控」（激活率 + engagement 对比 + slug 排行 + 解读准则）
- 脚本文件表加 `param-monitor.ts` + `_owner_exclusion.ts`

**`0b41c6d` 扩 §8.5 per-param 切片**（GA4 admin 5/16 注册新维度后）：
- 按 `param_id` 聚合：哪个参数被改最多
- Top 用户偏好（slug × param_id × value）：用户改成什么值

容错：GA4 不 backfill 5/16 前的事件，脚本检测到 `(not set)` 时输出「⏳ 等新事件」提示而非空表。

---

## Part D — 新参数化 config (`695e4d3`)

从「非参数化 top-N，sessions ≥ 5 且内容是 SD/MJ tag-style」候选中筛 3 个：

| slug | 7d sessions | params |
|---|---|---|
| anime-prompt-f061b77b | 12 | hairLength + palette |
| anime-girl-scene-10 | 6 | style (artist) + palette + finish (texture) |
| anime-woman-neon-08 | 6 | era + subject + outfit (color) |

跳过：
- wedding-shiromuku-japanese / straw-hat-mirror-sunglasses-summer → narrative 长 prompt，**不适合参数化** (per `project_prompt_params.md` 原则)
- anime-char-neon-07 / business-suit-woman / elegant-dress-woman → sessions 较低，下次回查再考虑

参数化覆盖：14 + 2 alias = 16 → **17 + 2 alias = 19 slug**。

audit ✓ 17 configs / 0 problems / 0 warnings · typecheck ✓。

---

## GA4 自定义维度状态（5/16 全部注册完）

| 维度名 | event parameter | scope | 注册日 |
|---|---|---|---|
| tool | tool | Event | 5/10 |
| trigger | trigger | Event | 5/10 |
| signed_in | signed_in | Event | 5/10 |
| **prompt_slug** | prompt_slug | Event | **5/16** |
| **param_id** | param_id | Event | **5/16** |
| **param_value** | param_value | Event | **5/16** |
| **prompt_category** | prompt_category | Event | **5/16** |

GA4 不 backfill：5/16 前的 45 个 prompt_param_changed 事件 prompt_slug 永远 `(not set)`。5/16+ 新事件自动有正确值。

---

## 用户必须手动做的待办

- ⏳ **5/19** 第 2 周回查日报，看 param-monitor §8 是否出现 per-param 切片（5/16-5/19 新调参事件填充）
- ⏳ **5/19** 看新 3 个 anime config 是否进入「已调参 slug 排行」
- ⏳ **5/22** 头部词进 25-35 = Tier 1+2 成功；仍 >50 需要 Tier 3
- ⏳ **5/23 / 5/24** 低 CTR 页两批 CTR ≥ 5% 回查
- ⏳ outfit-swap Phase 0 R&D Spike（25 张实测 + 三轴评估）
- ⏳ Stripe Dashboard 完善（Public name / Support / Terms / Privacy URL）
- ⚠️ 轮换 Google OAuth Client Secret（2026-05-05 泄露至今未处理）

---

## 已沉淀的 memory / SKILL 更新

- SKILL.md 加 §8 + §8.5「Prompt 参数化效果监控」节
- SKILL.md 脚本表加 `param-monitor.ts` + `_owner_exclusion.ts`
- `_owner_exclusion.ts` 共享清单（避免下次又遗漏 owner 邮箱）
- `param-monitor.ts` 用 `getConfiguredSlugs()` 而非手维护，新加 config 自动纳入监控

## 关键决策点（用户洞察）

> "copy转化提升这个本身也符合逻辑，因为用户只需要最想要的提示词，他并不一定会多次尝试同一个提示词吧。但是调参的使用量已经说明了这个功能是有价值的吧。"

→ copy-rate 不是参数面板的合适指标。换成 engagement 后参数化的价值清晰可见（bounce -8.6pp / engage +8.6pp）。**memory 决策准则需要更新**：5% activation 只是「至少被触发」最低门槛，真正「有用」信号是 engagement 改善。

---

## Part F — /collect-content 一轮（lexica 中止 + midjourney 15 入库 + tag 审批）

### F1 — lexica 中止

5/16 下午跑 `/collect-content` 默认 source=lexica + per=8。结果：
- **42 条抓取 / 31 条重复（74% 重复率）** — lexica 默认 8 关键词已被多次抓过
- 剩 11 条中 8 条短到无信息（"portrait stock photography" 3 词 / "unreal engine render of Jesus"）
- 3 条 "asian videographer making-of" 主题（#7 / #10 / #11 内容近乎逐字相同）
- Haiku 分类后 3 useful 全是 `costume` 类 → 但 costume 当前 DB 62 条（最高分类）priority 仅 10 + 40% 上限 → **入库 = 0 条**

**决策**：中止 lexica，切到 midjourney（HF 3K 数据集，前几次只采过 ~50-70 条，余量大）。

### F2 — midjourney offset=784 段 15 入库

- 抓取 100 条 / **0 重复**（offset=784 从未碰过）
- Haiku 分类后 100/100 useful（MJ 内容质量稳定高），分布：creative 35 / background 20 / costume 16 / clothing 6 / color 6 / cosplay 2 / camera 2 / anime 1
- SEO 优先级筛 top 15 + 单分类 40% 上限：**clothing 6 + cosplay 2 + color 6 + camera 1 = 15** — 全是高 priority 分类
- 修了脚本里 `creative` priority 误用 text 系 40（应该画像系 3）的 bug
- 15/15 图片下载 + Blob 上传 + DB 写入 全 OK

### F3 — Phase 7.5 Tag Approval Gate

采集后未审核 tag 状态：
- 总 564 / approved 141 / noindex 423 (75.0%) — 与 5/12 R2 后 74.8% 基本持平 ✅ 没反弹
- 累计 ≥3 prompt 的 🟢 推荐批准:
  - `カラー` (5 prompts) — R2 漏批
  - `油彩` (3 prompts) — 新 tag，艺术媒介
  - `赤髪` (3 prompts) — 新 tag

**用户授权 → 全部 3 个批准**:
- inline 跑 prisma update（不专门做 R3 脚本）→ noindex 423 → 420
- 跑 `_tag_seo_backfill.ts` → 3 tag seoIntro 全部生成（Gemini Flash，~$0.001）
- 验证 `/tag/カラー` `/tag/油彩` `/tag/赤髪` 全部 HTTP 200

### F4 — 入库 15 条 URL

```
clothing (6):
  /prompt/snowy-forest-lovers-oil-painting
  /prompt/elegant-winter-couple-ornate-clothing
  /prompt/retro-diner-uniform-women
  /prompt/white-gold-dress-red-haired-portrait
  /prompt/red-leather-blonde-woman-bold
  /prompt/black-leather-serious-woman-closeup
cosplay (2):
  /prompt/joker-cosplay-brown-coat-character
  /prompt/joker-beige-suit-bench-character
color (6):
  /prompt/dramatic-red-light-blonde-portrait
  /prompt/gray-lace-silver-haired-elegant
  /prompt/colorful-paint-thoughtful-elderly-man
  /prompt/urban-taxi-cityscape-serene-woman
  /prompt/red-white-striped-blonde-studio
  /prompt/children-playing-wet-street-colorful
camera (1):
  /prompt/dark-room-dramatic-lamp-tense
```

### F5 — 复盘 / 反向警示

- **lexica 默认 keywords 已到饱和点**（连续 3-4 轮 74%+ 重复率）→ 下次必须换关键词或换源
- **costume 已严重过载**（62 条 / priority 10 最低）→ 算法实际工作正常，无意外
- **HF MJ 数据集仍有 ~2K 条余量**（offset 884+ 段未碰）→ 下次首选

---

## Part G — Batch A: 参数化候选池评估 + 19+3 扩量 (`f4a99bd`)

### G1 — 候选池规模评估

跑了 `_param_candidates.ts`（28d 窗口）分析全 521 prompt：

| 池 | 数量 |
|---|---|
| Narrative 类（photo-edit/text）跳过 | 150 |
| Tag-style 可参数化基数 | 371 |
| 已参数化 | 19 |
| Tier 1 (≥5 sess/28d) 候选 | 29 |
| Tier 2 (2-4 sess) | 75 |
| Tier 3 (1 sess) | 73 |

**长期上限**：50-80 slug 是合理目标（超过后边际 ROI 急剧下降）。当前覆盖 19 → 第一波目标 ~40。

### G2 — 关键发现：alias 假设大部分是错的

原以为 6 个 `red-cocktail-dress-elegant-{sxsb,xut5,4bys,n4mi,zfui}` 是同一 prompt 复本可以 alias，**实际查内容后 5 个 slug 是完全不同主题**：
- canonical: 真红色 cocktail dress 标准内容
- -sxsb / -n4mi: Tokyo woman 酒店套房 office attire
- -xut5 / -4bys: 红毯 drawing 主题
- -zfui: school uniform Tokyo lady

→ 这 5 个跳过本批。

但发现新的 alias 机会：
- `anime-girl-red-14` / `anime-girl-scene-12` 与已有的 `anime-prompt-f061b77b` 几乎逐字相同（只差 "FULLY COLORED" vs "black and white"）→ alias
- `anime-girl-meadow-22` 与 `anime-girl-meadow-21` 大部分内容相同（前者带 "Chapter 10 Sharing the Treasurz" 噪声）→ alias

### G3 — 19 独立 config 设计

按候选池 Tier 1 筛选（去掉过长 narrative-ish 内容如 `straw-hat-summer` 1016 chars / `dark-fantasy-tavern-warrior` 1532 chars），19 个独立 config + 3 alias 整合。

设计原则：
- 每 config 2-3 个 param
- match string 必须在 content 中**精确**出现且**仅出现一次**（audit 强制）
- 默认 value 必须包含在 options 列表中（用 `withDefault` 或手写）
- 优先复用 `options.ts` 的 `hairColorOptions` / `fabricColorOptions` 等辅助函数
- 衣物色用 `fabricColorOptions(suffix)` 但 suffix 必须是 content 中的 noun（如 `dress` / `jacket`），不能是整个 noun phrase

### G4 — 踩坑 + 修复

- ⚠️ `anime-girl-meadow-21` 中 `blue shorts` 在 content 出现 2 次（"a pair of blue shorts" + "wearing blue shorts"）→ audit warning → 改 match 为 `a pair of blue shorts` 唯一
- ⚠️ 改后用 `fabricColorOptions('pair of blue shorts')` 会生成 "red pair of blue shorts" 语法错 → 改成自定义 inline options
- ⚠️ TypeScript unused imports (4 个 config) → 删除
- ⚠️ `_param_candidates.ts` 用了 ad-hoc 类型导致 tsc 报错 → 直接删除（一次性脚本不保留）

### G5 — 最终通过

- `sync.ts` → `36 configs`（17 original + 19 new）
- `audit.ts` → **36 configs / 0 problems / 0 warnings** ✓
- `tsc --noEmit` ✓

### G6 — 22 slug 净增清单

19 独立 config + 3 alias = 22 slug。新 alias:
```ts
'anime-girl-red-14': 'anime-prompt-f061b77b',
'anime-girl-scene-12': 'anime-prompt-f061b77b',
'anime-girl-meadow-22': 'anime-girl-meadow-21',
```

总覆盖 **19 → 41 slug**。

### G7 — 预期效果

部署后 5/19 第 2 周回查时：
- 参数化页面 28d sessions: 95 → ~280-320（3x）
- 41 slug 覆盖了 Tier-1 95% 的高流量
- bounce -8.6pp / engage +8.6pp 差异在 ~300 sessions 下应该统计上更稳定（从 borderline 显著 → clear 显著）
- §8.5 per-param 切片应首次出真数据（5/16 维度注册后到 5/19 累计 3 天事件）

### G8 — 没做的 / 留到下次

- **5 个 red-cocktail-dress-elegant 复本 slug**（累计 23 sess）— 各自内容不同需 careful，留下批
- **business-suit-woman 系列也有一些复本**（已发现 -8s35 + canonical 内容不同所以做了 2 个独立 config）
- **Tier 2 (2-4 sess) 75 个候选**未碰
- **Tier 3 (1 sess) 73 个候选**未碰
- **photo-edit 类完全不做**（narrative，per memory `project_prompt_params.md` 原则）
- **anime-char-neon-07** (12 sess) 没做，因为内容是 "punk doge grandma" niche 一次性主题，参数化收益低

---

## 当日最终交付总览

| 维度 | 数据 |
|---|---|
| Commit | 5 个推到 main（不含 collect-content 的 DB-only 改动）|
| 参数化覆盖 | **19 → 41 slug**（+22）|
| Audit 状态 | 36 configs / 0 problems / 0 warnings |
| 新入库 prompt | 15 条 Midjourney（clothing/cosplay/color/camera 高 priority 分类）|
| Approved tag | 141 → 144（+3：カラー / 油彩 / 赤髪）|
| noindex 占比 | 75.0% → 74.5%（持续稳定，没反弹）|
| 新 GA4 自定义维度 | 4 个（prompt_slug / param_id / param_value / prompt_category）|
| 新 SKILL.md 节 | §8（参数化效果监控）+ §8.5（per-param 切片）|
| 新 memory | feedback_param_metric_engagement.md / feedback_owner_test_exclusion.md |
| Bug 修复 | PaywallTrigger 分流 / owner exclusion 漏邮箱 / plumbing 污染识别 |
| 站点 prompt 总数 | 506 → **521** |
